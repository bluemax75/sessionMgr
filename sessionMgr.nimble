# Package

version       = "0.1.0"
author        = "charly"
description   = "Session control app for openbox"
license       = "MIT"

# Dependencies

requires "nim >= 0.14.0"
requires "gtk2 >= 1.0"

bin = @["sessionMgr"]
